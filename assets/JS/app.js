jQuery("#responsive_headline").fitText();
wow = new WOW(
    {
    boxClass:     'animate__animated',      // default
  }
  )
  wow.init();
$(function() {  
    $("body").niceScroll(
      {
        cursorcolor: "black",
        cursorwidth: "5px"
      }
    );
});